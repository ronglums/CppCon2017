{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build with gcc",
            "type": "shell",
            "command": "${workspaceRoot}/.vscode/gccbuild.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}